<%= simple_form_for(@puppy) do |f| %>
  <%= f.input :name %>
  <%= f.input :breed %>
  <%= f.input :age %>
    <%= f.input :photo %>

  <%= f.input :photo_cache, as: :hidden %>
<!--   <%= image_tag(@puppy.photo_url) if @puppy.photo? %>
  <%= f.file_field :photo %>
  <%= f.hidden_field :photo_cache %> -->
  <%= f.button :submit %>
<% end %>
